# Desa-Kita
Simple FastAPI web application that displays village data by district. Built as a learning project to explore routing, templates, and structured geographic data

A simple FastAPI web application that displays village data by district.

## Features
- FastAPI backend
- Jinja2 templates
- Dynamic village pages
- Hierarchical data structure (district → village)

## Run

uvicorn main:app --reload
